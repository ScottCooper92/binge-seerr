# A Composable is PascalCase by convention, which ktlint's function-naming rule has to be told;
# everything else stays at ktlint's defaults, so no existing file reformats.
[*.{kt,kts}]
ktlint_function_naming_ignore_when_annotated_with = Composable
