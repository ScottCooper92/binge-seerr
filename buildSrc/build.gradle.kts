// The one build-logic type this repository carries: the translation-staleness task, ported from
// Binge's convention plugin. Everything else configures the app module directly.
plugins {
    `kotlin-dsl`
}

repositories {
    mavenCentral()
}
